// Sidebar navigation shell.
// Navigation items are added per user role in Phase 2.
export function Sidebar() {
  return (
    <aside className="w-64 shrink-0 border-r border-border bg-background">
      {/* Role-specific nav links — Phase 2 */}
    </aside>
  )
}
