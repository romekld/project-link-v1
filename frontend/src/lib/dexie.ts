// Dexie.js (IndexedDB) database instance for offline-first PWA storage.
// TODO: npm install dexie, then replace this stub:
//
//   import Dexie, { type Table } from 'dexie'
//   import type { PatientVisit } from '@/features/bhw'
//
//   export class LinkDatabase extends Dexie {
//     patientVisits!: Table<PatientVisit>
//     constructor() {
//       super('link-db')
//       this.version(1).stores({ patientVisits: '++id, status, health_station_id' })
//     }
//   }
//   export const db = new LinkDatabase()

export {}
